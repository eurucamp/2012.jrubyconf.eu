---
body_class: index
---

%article
  %header
    %h2
      JRubyConf’s first European excursion
    %h3
      17 August 2012, Berlin

  = wrapped :section, :class => 'register' do
    = link_to 'http://eurucamp2012.eventbrite.com', :title => 'Get your ticket', :class => 'primary' do
      %span
        Register now
    %p
      %small
        Ticket prices are
        = link_to 'starting from &euro;50', 'http://2012.eurucamp.org/2012/07/05/ticket-sales-begin-monday', :title => 'More information on our blog'
        \.

  .block
    %section.speakers
      = wrapped :header do
        %h3 Our speakers
      = wrapped do
        %ul
          - data.speakers.each do |speaker|
            %li
              %img(src="/images/content/speakers/#{speaker.image}" alt="Portrait: #{speaker.name}")
              %span
                %strong= speaker.name
                = speaker.company

      %p.cfp
        If you want to speak at JRubyConf EU
        %a(href="https://eurucamp2012.busyconf.com/proposals/new" title="Call for proposals") you can still submit a proposal

    %section.twitter-feed
      = wrapped :header do
        %h3 Lately on Twitter
      = wrapped do
        .tweets
          .loading
            Loading tweets, sit tight &hellip;
          %noscript
            Sorry! We have been lazy and are using Javascript to fetch the latest tweets.
            %br
            Head over to
            %a(href="https://twitter.com/eurcuamp/" title="@eurucamp") @eurucamp on Twitter
            instead?

    %section.attendees
      = wrapped :header do
        %h3 Attending
      = wrapped do
        %ul
          - data.attendees.each do |id|
            %li
              %a(href="https://twitter.com/#{id}" title="View #{id} on Twitter")
                %img(src="https://api.twitter.com/1/users/profile_image/#{id}" alt="Avatar for #{id}")
        %p.more
          If you are attending
          %a(href="http://lanyrd.com/2012/jrubyconfeu" title="JRubyConf EU on lanyard.com") Let us know on Lanyrd

    = partial 'partials/sponsors', :locals => { :sponsors => data.sponsors }

    = wrapped :header do
      %h3 Our partners
    = partial 'partials/partners', :locals => { :partners => data.partners }
